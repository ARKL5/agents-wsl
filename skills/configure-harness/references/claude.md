# Claude Code

## 全局指令

- `~/.claude/CLAUDE.md`
- `~/.claude/agents/swift.md`
- `~/.claude/agents/deliberate.md`
- 现查：Claude Code 官方 sub-agents / CLAUDE.md 文档（`find-docs`）

## 表面

- `~/.claude/settings.json`（`permissions`、`disable*`）
- 功能裁剪：现查官方 settings / permissions 文档和这份 live 配置，把能关的面列给用户，由用户决定。不要为量尺改 `ANTHROPIC_BASE_URL`。
- `model` / `effortLevel` 是活偏好，除非用户这轮要改，否则原样留下

## 家私

调用词和 agent frontmatter 现查。不要改插件市场源、hooks、密钥。
